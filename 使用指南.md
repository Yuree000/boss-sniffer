# BOSS Sniffer 使用指南

> v0.18.0 · 2026-05-19（v0.17.1.x 沟通页评估闭环 + v0.18.0 死代码清理）

## 1. 这个扩展能帮你做什么

BOSS 直聘候选人**自动评估** + **自动招呼** + **沟通页评估自动求简历**。

两个场景：

**A. 推荐页（v0.16.0 起跑通）**
打开推荐页 → 点「开始本轮」→ 扩展自动抓 15 人一批 → 大模型按你设的 JD 条件判定「符合 / pass」→ 符合的自动招呼候选人，pass 的标不合适。HR 全程不用一个个看，等结果就行。

**B. 沟通页（v0.17.1.x 起跑通）**
切到 BOSS「沟通」→「新招呼」tab → sidepanel 「扫描本页候选人」 → 「一键评估」（批量自动）或单独点 ⚡ 评估某个 → 评估为「符合」的人：
- **单评**：永远停在 sidepanel 等 HR 决策，HR 看完结果点卡片旁 🎯 话术+求简历 按钮决定
- **批量**（admin 勾「启用批量评估自动求简历」开关）：自动**输入预设话术** → 点求简历 → 弹窗确定，候选人收 2 条消息（HR 寒暄 + BOSS 默认「方便发一份你的简历过来吗？」）

每个候选人扫描时会**自动点开「在线简历」弹窗**抓 iframe 里的完整简历正文（含简介/语言/技能/完整工作经历）喂给 LLM，避免详情面板字段稀疏导致的误判（如外籍候选人「精通印尼语」这类详情面板没列出但简历正文里有的问题）。

---

## 2. 安装步骤（首次 4 步）

1. **解压**：把收到的 zip 解压到任意目录（比如桌面 `BOSS-Sniffer/`）。**注意路径不要带 `.`（点）开头的隐藏目录**，否则 Chrome 拒绝加载。若出现加载拓展不成功，检查是否 加载错了目录层级，确保是最内层。
2. **打开扩展页**：地址栏输入 `chrome://extensions/`，回车
3. **开启开发者模式**：页面右上角拨开关
4. **加载扩展**：左上角点「加载已解压的扩展程序」→ 选第 1 步解压的目录

工具栏出现 BOSS Sniffer 图标 ＝ 安装成功。固定到工具栏（拼图图标 → 选 BOSS Sniffer 旁边的图钉）方便后续点击。

---

## 3. 首次配置（2 步）

### Step 1 配大模型

1. 点工具栏 BOSS Sniffer 图标 → 侧边栏打开
2. 侧边栏底部「⚙️ 设置」→ 弹出 admin 配置页
3. 「大模型配置」区 → 点「+ 新增大模型」
4. 厂商选 **Wiz（公司中转，推荐）**
5. 模型选 **Haiku 4.5 · 快 · 便宜**
6. 填 Wiz API Key
7. 点「测试连接」→ 看到 ✓ 绿色 → 点「保存」
8. 列表里出现这条配置，左侧标「●当前」

### Step 2 选 JD

1. 关 admin 回侧边栏
2. 侧边栏顶部「当前 JD」下拉 → 默认有 3 个内置 JD（**印尼语实习生** / 测试工程师 / AI CX）
3. 选一个适合你当前招聘岗位的；或回 admin「JD 模板管理」区新建自己的岗位模板（点「新建 JD」→ 填岗位名 + 必要条件 M1..Mn + 可选条件 O1..On）

---

## 4. 日常使用流程

```
① 打开 BOSS 直聘推荐页（沟通页 / 牛人推荐都行）
② 侧边栏「本轮 tab」选「推荐」或「最新」
   - 推荐 = BOSS 算法挑过的池子
   - 最新 = 新发布的冷池子（没人抢过）
③ 填「招呼数」或「浏览数」（至少填一个）
   - 招呼数 = 评估「符合」自动招呼上限（达到就停）
   - 浏览数 = 评估候选人总数上限（达到就停）
④ 「自动招呼」勾选（一般开着）
⑤ 点「开始本轮」
```

扩展开始工作：

- 自动滚动 BOSS 页面 → 抓候选人 → LLM 评估 → 「符合」就发招呼
- 侧边栏实时显示评估卡（绿色 = 符合，灰色 = pass）
- 累计到目标就自动停

**看效果**：

- **当下**：侧边栏卡片（点姓名跳到 BOSS 页面那个人；点卡片右侧 ▶ 展开 pass 主因）
- **历史**：admin 顶部「打开看板」→ 看漏斗 + Pass 主因分布

---

## 5. FAQ

| 问题                 | 答                                                                    |
| ------------------ | -------------------------------------------------------------------- |
| 评估卡一直显示「评估中」不出结果   | 检查 LLM Key 是否正确：admin → 列表里那条配置 → 点「测试」。Key 不对 / Wiz 中转挂了 / 余额不足都会卡住 |
| 候选人评估完全不动          | 看「当前 JD」下拉是否选了；没选不会评                                                 |
| 我想看上一轮 / 历史候选人     | 取消勾选「仅显示本轮候选人」复选框                                                    |
| 自动招呼太快 / 太慢        | admin → 自动打招呼区 → 调「单条间隔」（默认 1.5-5 秒随机）                               |
| 看板顶部「数据健康」红了       | 重新加载扩展（chrome://extensions/ → 刷新按钮）后再开看板                             |
| 一次能跑多少人            | 你设的「招呼数」/「浏览数」上限即终止；其中一个先达到就停                                        |
| 数据存哪里？卸载扩展会丢吗      | 数据在你浏览器本地（IndexedDB）。卸载/重装会丢；JD 模板可在 admin 用「导出 JD 模板 JSON」备份        |
| 换电脑还能用吗            | 重装 + 重新填 LLM Key；JD 模板可导出 JSON 备份再导入                                 |
| 我想暂停 / 提前停止        | 点侧边栏「停止本轮」按钮（运行中会变红色）                                                |
| 评估「pass」的人为什么 pass | 卡片右侧 ▶ 展开 → 显示哪个必要条件没过 + LLM 解释                                      |
| Chrome 之外的浏览器能用吗   | Edge 可以（同 Chromium 内核）；Firefox / Safari 不支持                          |

---

## 6. 已知限制

- ✅ ~~沟通页一键操作（求简历 / 标不合适）~~：v0.17.1.x 已交付
- ✅ ~~完整简历字段~~：v0.17.1.2 起从「在线简历」弹窗 iframe 抓 resumeFullText，含简介+语言+技能+完整经历
- ❌ **沟通页聊天历史**：扩展暂未接通候选人在 BOSS 聊天里说的话（BOSS 改协议在排查）
- ❌ **跨电脑数据自动同步**：v0.17.0 起 JD 模板 + 话术模板 + LLM 配置走 chrome.storage.sync，登录同 Chrome/Edge 账号可跨设备；但评估结果 / 抓包数据仍在 IndexedDB 本机
- ❌ **过期数据自动清理**：7 天前的原始抓包数据会自动删，评估记录保留

## 7. 沟通页评估 + 自动求简历 流程（v0.17.1.x 新增）

### 7.1 准备

1. admin → **话术模板管理** section → 默认有 3 条 SEED（通用礼貌型 / 简洁型 / 自我介绍型），HR 可改可加
2. sidepanel 顶部「当前话术」下拉 → 选要发的那条
3. （可选）admin → **评估后自动求简历** section → 勾「启用批量评估自动求简历」（仅批量场景生效，单评永远手动）
4. （可选）勾「试跑模式 (dry-run)」：跑通所有定位但不点最后的发送/确定，BOSS UI 改时调试神器

### 7.2 单评流程（HR 看个体看仔细）

1. sidepanel 切「沟通页」tab → 点「🔍 扫描本页候选人」（虚拟列表，扫 5-10 秒）
2. 单评：点候选人卡片旁的 ⚡ 评估按钮
3. 扩展自动：DOM 模拟点击卡片 → 扫详情面板 → 点击「在线简历」抓 iframe → ESC 关弹窗 → LLM 评估 → 出结果
4. 结果出现在 sidepanel：
   - 「符合」→ 卡片右侧出现 🎯 话术+求简历 按钮 → **HR 自己决定**点不点
   - 「pass」→ 卡片右侧出现 🎯 标不合适 按钮 → HR 决定
5. 点 🎯 话术+求简历 → 扩展自动：选中候选人 → 输入当前话术 → 发送 → 1-2s 拟人停顿 → 点求简历 → 弹窗确定

### 7.3 批量流程（HR 不想一个个看）

1. 同上扫候选人
2. admin 勾「启用批量评估自动求简历」开关（默认关）
3. 点「⚡ 一键评估」批量评估
4. 扩展自动 per 候选人 串行：扫详情 → 扫在线简历 iframe → LLM 评估 →（如「符合」+ 开关 ON）自动输入话术 + 求简历
5. 候选人间冷却 2-4s 拟人节奏

### 7.4 风控行为

- 单批 DOM 扫描上限默认 1（admin 可调，跑通 1 人确认安全后改 5/15）
- 串行执行，BOSS 同时只看一个详情面板，与真人 HR 节奏一致
- 发话术 → 求简历 中间有 1-2s 拟人停顿
- 连续 3 个候选人 action 失败自动停（疑似 BOSS UI 变 / 风控触发）
- 评估循环进行中，sidepanel 手动 🎯 会被互斥保护拒绝

---

## 8. 按钮 / 控件速查

### 侧边栏 — 沟通页 tab（v0.17.1.x 新增）

| 按钮 / 控件 | 作用 |
|---|---|
| **当前话术** 下拉 | 选要发的话术模板（admin 加） |
| **🎯 批量自动 ON** 徽章 | 提示 admin 开关已勾，仅批量评估时自动求简历 |
| **🔍 扫描本页候选人** | 扫沟通页虚拟列表（滚动 5-10 秒），扫到的进池子 |
| **⚡ 一键评估** | 批量评估池子里所有候选人（受 admin 开关控制是否自动求简历） |
| 候选人卡片 — **⚡ 评估** 按钮 | 单评该候选人（永远不自动求简历） |
| 候选人卡片 — **🎯 话术+求简历** 按钮 | 评估「符合」后出现，HR 主动点 → 发话术+求简历，**始终真实执行**（忽略 admin dryRun） |
| 候选人卡片 — **🎯 标不合适** 按钮 | 评估「pass」后出现，点 → BOSS 标不合适，候选人卡片消失 |
| **■ 停止** | 中止批量评估循环 |
| **🐛 操作调试日志** | 展开看每步 step + ok/失败原因，调 BOSS UI 变化时神器 |

### 侧边栏 — 推荐页 tab（日常用）

| 按钮 / 控件                     | 作用                                          |
| --------------------------- | ------------------------------------------- |
| **当前 JD** 下拉                | 选这一轮用哪个岗位模板去评估候选人                           |
| **本轮 tab** 下拉               | 选「推荐」或「最新」                                  |
| **招呼数** 输入框                 | 评估「符合」累计达到这个数就停（自动招呼上限）                     |
| **浏览数** 输入框                 | 候选人总评估数上限（含 pass）；两个数任一先达到就停                |
| **自动招呼** toggle             | 关掉则只评估不发招呼（适合试运行）                           |
| **开始本轮** / **停止本轮**         | 启动 / 中止流程                                   |
| **仅显示本轮候选人** checkbox       | 默认勾选只看这一轮；取消勾选看全部历史                         |
| 候选人卡片 — 点**姓名**             | BOSS 页面平滑滚动到这个人并高亮 1.5 秒                    |
| 候选人卡片 — 点 **▶ 箭头**（pass 才有） | 展开看「为什么 pass」+ LLM 详细判断                     |
| **⚙️ 设置**                   | 打开 admin 配置页                                |
| **刷新**                      | 立刻拉取最新数据（不点也会每 1.5 秒自动刷一次）                  |
| **清空评估**                    | 清当前侧边栏卡片；不影响看板漏斗、招呼历史、抓包数据                  |
| **清空全部**                    | 清掉当前列表所有数据（评估 + 抓包 + 招呼记录），保持页面清爽，不会影响看板数据。 |

### admin 配置页（v0.17.1.x 新增 section）

**话术模板管理** + **评估后自动求简历** 两个 section 是 v0.17.1.0 新增：

| 区域 | 按钮 / 控件 | 作用 |
|---|---|---|
| **话术模板管理** | + 新建话术 / 编辑 / 删除 | CRUD 话术（要 ≥5 字符），sidepanel 选当前用哪条 |
| **评估后自动求简历** | 启用批量评估自动求简历 | 开了 batch 评估自动跑话术+求简历（单评始终手动） |
| | 试跑模式 (dry-run) | 跑通所有定位但不点最后的发送 / 确定，调试 BOSS UI 改名用 |
| | 候选人间冷却下限 / 上限（毫秒） | 自动模式专属冷却（默认 2-4s） |

### admin 配置页 — 原有 section

| 区域                | 按钮 / 控件              | 作用                                  |
| ----------------- | -------------------- | ----------------------------------- |
| 顶部                | **打开看板**             | 跳转到数据看板（漏斗 + Pass 主因）               |
| **大模型配置**         | **+ 新增大模型**          | 加新的 LLM 配置（Wiz / DeepSeek / 自定义）    |
|                   | 列表条目「**设为当前**」       | 切换使用哪个 LLM 评估（即时生效）                 |
|                   | 列表条目「**测试**」         | 测试这条配置能否调通 LLM                      |
|                   | 列表条目「**编辑**」         | 改这条配置                               |
|                   | 列表条目「**删除**」         | 删这条配置                               |
|                   | drawer「**测试连接**」     | 当前 form 字段临时测试一次（不写入列表）             |
|                   | drawer「**保存**」       | 保存当前编辑的 LLM 配置（实时写入）                |
|                   | drawer「**取消**」       | 关闭抽屉不保存                             |
| **JD 模板管理**       | **新建 JD**            | 新建岗位模板（必要条件 M + 可选条件 O + 阈值 K）      |
|                   | 列表行「**编辑** / **删除**」 | 改 / 删某条 JD                          |
|                   | 列表行「●**当前**」标记       | 标记当前 JD（侧边栏切换的同步显示）                 |
|                   | 「**预览 prompt**」      | 看选中 JD 实际给 LLM 的 system prompt 长什么样 |
| **自动打招呼（保持默认即可）** | **单条间隔（最小 / 最大）**    | 招呼之间随机间隔（默认 1.5-5 秒）                |
|                   | **每 N 个休息**          | 每发 N 个招呼后停一会儿（默认 30 个）              |
|                   | **休息（分钟）**           | 休息时长（默认 5-10 分钟随机）                  |
| 顶部 / 底部           | **保存所有更改**           | 保存 sayHi 参数（LLM 配置已实时保存，不需要这个按钮）    |
|                   | **导出 JD 模板 JSON**    | 备份所有 JD 模板到文件                       |
|                   | **导入 JD 模板 JSON**    | 恢复 JD 模板                            |

### 看板页面

| 按钮 / 控件             | 作用                                       |
| ------------------- | ---------------------------------------- |
| **今天 / 7 天 / 30 天** | 切换时间窗                                    |
| **JD 下拉**           | 看哪个岗位的数据                                 |
| **刷新**              | 立刻重新统计                                   |
| 视图一                 | 候选人池 → 评估 → 符合 → 招呼 → 互动 → 收简历 漏斗        |
| 视图三                 | Pass 候选人按「主因」分组（M1 不达 / M2 信息缺 / 可选不足 等） |

---

**完。**

# BOSS Sniffer 使用指南

> v0.22.3 · 2026-05-20（Phase 1 多岗位 JD 路由 + Phase 2 沟通页 UI 大改完工）

## 1. 这个扩展能帮你做什么

BOSS 直聘候选人**自动评估** + **自动招呼** + **沟通页评估自动求简历**。

两个场景：

**A. 推荐页（v0.16.0 起跑通）**
打开推荐页 → 点「开始本轮」→ 扩展自动抓 15 人一批 → 大模型按你设的 JD 条件判定「符合 / pass」→ 符合的自动招呼候选人，pass 的标不合适。HR 全程不用一个个看，等结果就行。

**B. 沟通页（v0.17.1.x 起跑通；v0.21-v0.22 大改）**
切到 BOSS「沟通」→「新招呼」tab → sidepanel 沟通页 tab 顶部 control-bar 点「▶ 开始处理本批」（v0.22 起合并扫描 + 评估为一键），扩展自动按候选人「沟通职位」匹配到对应的 JD 评估（v0.21 多岗位路由）：
- **多岗位路由**：HR 同时挂多个岗位时，沟通页候选人按"应聘岗位"自动匹配 JD。要求每个 JD 在 admin 配「BOSS 沟通职位」别名（默认 SEED 已配）
- **未识别岗位**：候选人沟通职位未匹配任何 JD 别名时，显示黄底卡片「未识别 · 已跳过评估」（不消耗 LLM 配额）
- **自动操作 checkbox**（默认 OFF）：勾「自动话术+求简历」后评估「符合」的人自动发话术 + 求简历；勾「自动标不合适」**仅记录配置不执行**（Phase 3 加 30 秒撤销窗口后才接入实际执行）
- **K/N 阈值**：浏览数 K = 本批最多评估几人；招呼数 N = 本批最多发几条话术（仅自动模式生效）；任一达上限即停整批
- **测试模式**：扫描 / 一键评估 / 停止 / 清空池子 / 调试日志 等分步按钮折叠到底部「🧪 测试模式」区，bug 排查 / 单步验证用

每个候选人扫描时会**自动点开「在线简历」弹窗**抓 iframe 里的完整简历正文（含简介/语言/技能/完整工作经历）喂给 LLM，避免详情面板字段稀疏导致的误判（如外籍候选人「精通印尼语」这类详情面板没列出但简历正文里有的问题）。

---

## 2. 安装步骤（首次 4 步）

1. **解压**：把收到的 zip 解压到任意目录（比如桌面 `BOSS-Sniffer/`）。**注意路径不要带 `.`（点）开头的隐藏目录**，否则 Chrome 拒绝加载。若出现加载拓展不成功，检查是否 加载错了目录层级，确保是最内层。
2. **打开扩展页**：地址栏输入 `chrome://extensions/`，回车
3. **开启开发者模式**：页面右上角拨开关
4. **加载扩展**：左上角点「加载已解压的扩展程序」→ 选第 1 步解压的目录

工具栏出现 BOSS Sniffer 图标 ＝ 安装成功。固定到工具栏（拼图图标 → 选 BOSS Sniffer 旁边的图钉）方便后续点击。

---

## 3. 首次配置（2 步）

### Step 1 配大模型

1. 点工具栏 BOSS Sniffer 图标 → 侧边栏打开
2. 侧边栏底部「⚙️ 设置」→ 弹出 admin 配置页
3. 「大模型配置」区 → 点「+ 新增大模型」
4. 厂商选 **Wiz（公司中转，推荐）**
5. 模型选 **Haiku 4.5 · 快 · 便宜**
6. 填 Wiz API Key
7. 点「测试连接」→ 看到 ✓ 绿色 → 点「保存」
8. 列表里出现这条配置，左侧标「●当前」

### Step 2 选 JD

1. 关 admin 回侧边栏
2. 侧边栏顶部「当前 JD」下拉 → 默认有 3 个内置 JD（**印尼语实习生** / 测试工程师 / AI CX）
3. 选一个适合你当前招聘岗位的；或回 admin「JD 模板管理」区新建自己的岗位模板（点「新建 JD」→ 填岗位名 + **BOSS 沟通职位**别名 + 必要条件 M1..Mn + 可选条件 O1..On）
4. **配 BOSS 沟通职位别名**（v0.21 多岗位路由用）：每个 JD 的「BOSS 沟通职位」段填你在 BOSS 端创建岗位时用的真实岗位名（一行一个，可加多个别名）。沟通页扫到候选人后用这个匹配到本 JD。**未配别名的 JD 在沟通页用不了自动操作 checkbox**（防错岗）。

---

## 4. 日常使用流程

```
① 打开 BOSS 直聘推荐页（沟通页 / 牛人推荐都行）
② 侧边栏「本轮 tab」选「推荐」或「最新」
   - 推荐 = BOSS 算法挑过的池子
   - 最新 = 新发布的冷池子（没人抢过）
③ 填「招呼数」或「浏览数」（至少填一个）
   - 招呼数 = 评估「符合」自动招呼上限（达到就停）
   - 浏览数 = 评估候选人总数上限（达到就停）
④ 「自动招呼」勾选（一般开着）
⑤ 点「开始本轮」
```

扩展开始工作：

- 自动滚动 BOSS 页面 → 抓候选人 → LLM 评估 → 「符合」就发招呼
- 侧边栏实时显示评估卡（绿色 = 符合，灰色 = pass）
- 累计到目标就自动停

**看效果**：

- **当下**：侧边栏卡片（点姓名跳到 BOSS 页面那个人；点卡片右侧 ▶ 展开 pass 主因）
- **历史**：admin 顶部「打开看板」→ 看漏斗 + Pass 主因分布

---

## 5. FAQ

| 问题                 | 答                                                                    |
| ------------------ | -------------------------------------------------------------------- |
| 评估卡一直显示「评估中」不出结果   | 检查 LLM Key 是否正确：admin → 列表里那条配置 → 点「测试」。Key 不对 / Wiz 中转挂了 / 余额不足都会卡住 |
| 候选人评估完全不动          | 看「当前 JD」下拉是否选了；没选不会评                                                 |
| 我想看上一轮 / 历史候选人     | 看板 4.E「候选人记录」（默认展示，含 CSV 导出）；sidepanel 只显示本轮候选人，由 START_LOOP 自动管理     |
| 自动招呼太快 / 太慢        | admin → 自动打招呼区 → 调「单条间隔」（默认 1.5-5 秒随机）                               |
| 看板顶部「数据健康」红了       | 重新加载扩展（chrome://extensions/ → 刷新按钮）后再开看板                             |
| 一次能跑多少人            | 你设的「招呼数」/「浏览数」上限即终止；其中一个先达到就停                                        |
| 数据存哪里？卸载扩展会丢吗      | 数据在你浏览器本地（IndexedDB）。卸载/重装会丢；JD 模板可在 admin 用「导出 JD 模板 JSON」备份        |
| 换电脑还能用吗            | 重装 + 重新填 LLM Key；JD 模板可导出 JSON 备份再导入                                 |
| 我想暂停 / 提前停止        | 点侧边栏「停止本轮」按钮（运行中会变红色）                                                |
| 评估「pass」的人为什么 pass | 卡片右侧 ▶ 展开 → 显示哪个必要条件没过 + LLM 解释                                      |
| Chrome 之外的浏览器能用吗   | Edge 可以（同 Chromium 内核）；Firefox / Safari 不支持                          |

---

## 6. 已知限制

- ✅ ~~沟通页一键操作（求简历 / 标不合适）~~：v0.17.1.x 已交付
- ✅ ~~完整简历字段~~：v0.17.1.2 起从「在线简历」弹窗 iframe 抓 resumeFullText，含简介+语言+技能+完整经历
- ❌ **沟通页聊天历史**：扩展暂未接通候选人在 BOSS 聊天里说的话（BOSS 改协议在排查）
- ❌ **跨电脑数据自动同步**：v0.17.0 起 JD 模板 + 话术模板 + LLM 配置走 chrome.storage.sync，登录同 Chrome/Edge 账号可跨设备；但评估结果 / 抓包数据仍在 IndexedDB 本机
- ❌ **过期数据自动清理**：7 天前的原始抓包数据会自动删，评估记录保留

## 7. 沟通页评估 + 自动求简历 流程（v0.22 control-bar 大改后）

### 7.1 准备

1. admin → **话术模板管理** section → 默认有 3 条 SEED（通用礼貌型 / 简洁型 / 自我介绍型），HR 可改可加
2. admin → **JD 模板管理** → 给每个 JD 配「BOSS 沟通职位」别名（多岗位路由用，见 §3 Step 2）
3. sidepanel 沟通页 tab control-bar 顶部「当前话术」下拉 → 选要发的那条
4. （可选）勾 control-bar 里的「自动话术+求简历」checkbox：评估「符合」时自动发话术 + 点求简历（默认关；JD 没配 bossJobNames 时强制禁用）
5. （可选）admin → **评估后自动求简历** section 仍保留全局「启用批量评估自动求简历」开关 + 「试跑模式 (dry-run)」（dry-run 跑通所有定位但不点最后的发送/确定，BOSS UI 改时调试神器）

### 7.2 主流程（v0.22 推荐用法 · HR 一键启动）

1. 切到 BOSS「沟通」→「新招呼」tab
2. 回 sidepanel 沟通页 tab，control-bar 配好：
   - 当前话术（如要自动发）
   - 浏览数 K（留空 = 全部未读 / 填数字 = 本批最多评估几人）
   - 招呼数 N（仅自动模式生效 / 留空 = 不限）
   - 自动话术+求简历 checkbox（按需勾）
3. 点 control-bar 的「▶ 开始处理本批」按钮
4. 扩展自动 per 候选人 串行：
   - 按沟通职位匹配到对应 JD（v0.21 多岗位路由）；未匹配则黄卡跳过
   - DOM 模拟点击卡片 → 扫详情面板 → 点击「在线简历」抓 iframe → ESC 关弹窗 → LLM 评估
   - 如「符合」+ 自动话术 checkbox ON → 自动输入话术 + 求简历
5. 卡片头部小字显示「沟通职位『X』→ JD: Y」，HR 能看清楚每个人路由到哪个 JD 评估
6. 任一阈值（K 浏览数 / N 招呼数）达上限自动停

### 7.3 单评流程（HR 看个体看仔细）

1. control-bar 跑完一批后，或想重新评估某人 → 卡片旁点 ⚡ 评估按钮
2. 单评**永远不自动求简历**（即使 checkbox 勾了），HR 看完结果手动决定：
   - 「符合」→ 卡片右侧出现 🎯 话术+求简历 按钮 → HR 自己决定点不点
   - 「pass」→ 卡片右侧出现 🎯 标不合适 按钮 → HR 决定
3. 点 🎯 话术+求简历 → 扩展自动：选中候选人 → 输入当前话术 → 发送 → 1-2s 拟人停顿 → 点求简历 → 弹窗确定

### 7.4 测试模式（debug 用，平时折叠）

sidepanel 沟通页底部「🧪 测试模式」`<details>` 折叠区里有：
- 🔍 扫描本页候选人（单独扫不评估）
- ⚡ 一键评估（对池子已扫的人评估，不重扫）
- ■ 停止
- 清空池子
- 🐛 操作调试日志（看每步 step + ok/失败原因，BOSS UI 改时神器）

平时不用展开。撞 bug 想分步走 / 看链路日志时展开。

### 7.5 风控行为

- 单批 DOM 扫描上限默认 1（admin 可调，跑通 1 人确认安全后改 5/15）
- 串行执行，BOSS 同时只看一个详情面板，与真人 HR 节奏一致
- 发话术 → 求简历 中间有 1-2s 拟人停顿
- 连续 3 个候选人 action 失败自动停（疑似 BOSS UI 变 / 风控触发）
- 评估循环进行中，sidepanel 手动 🎯 会被互斥保护拒绝

---

## 8. 按钮 / 控件速查

### 侧边栏 — 沟通页 tab（v0.22 control-bar 大改后）

**control-bar（顶部主流程区）**

| 按钮 / 控件 | 作用 |
|---|---|
| **当前话术** 下拉 | 选要发的话术模板（admin 加） |
| **🎯 批量自动 ON** 徽章 | 提示 admin 开关已勾 |
| **浏览数 K** 输入框 | 本批最多评估几人；留空 = 全部未读 |
| **招呼数 N** 输入框 | 本批最多发几条话术；仅自动话术+求简历勾选时生效；留空 = 不限 |
| **▶ 开始处理本批** 按钮 | 主入口：自动扫描 + 评估串行执行；按沟通职位路由到对应 JD |
| **■ 停止** 按钮 | 停止本批评估（已发起的 LLM 调用会跑完，未发起的跳过） |
| **☐ 自动话术+求简历** checkbox | 评估「符合」时自动输入话术 + 求简历。JD 未配 bossJobNames 时强制禁用 |
| **☐ 自动标不合适** checkbox | ⚠ Phase 2 阶段仅存配置不执行（Phase 3 加 30s 撤销窗口后才接入实际执行） |

**候选人卡片**

| 按钮 / 控件 | 作用 |
|---|---|
| 卡片头部 — **沟通职位『X』→ JD: Y** 小字 | 显示这个候选人路由到哪个 JD 评估（多岗位路由） |
| 黄底卡 — **🟡 沟通职位未识别 · 已跳过评估** | 沟通职位没匹配任何 JD bossJobNames 别名时显示。admin 加该别名后重评 |
| 卡片 — **⚡ 评估** 按钮 | 单评（永远不自动求简历，永远手动决策） |
| 卡片 — **🎯 话术+求简历** 按钮 | 评估「符合」后出现，HR 主动点 → 发话术+求简历，**始终真实执行**（忽略 admin dryRun） |
| 卡片 — **🎯 标不合适** 按钮 | 评估「pass」后出现，点 → BOSS 标不合适，候选人卡片消失 |

**🧪 测试模式折叠区（默认折叠，bug 排查用）**

| 按钮 / 控件 | 作用 |
|---|---|
| **🔍 扫描本页候选人** | 单独扫沟通页虚拟列表（5-10 秒），不连带评估 |
| **⚡ 一键评估** | 单独对池子已扫的人评估，不重扫 |
| **■ 停止** | 同 control-bar 停止 |
| **清空池子** | 清当前批的候选人池（不影响评估历史） |
| **🐛 操作调试日志** | 展开看每步 step + ok/失败原因 |

### 侧边栏 — 推荐页 tab（日常用）

| 按钮 / 控件                     | 作用                                          |
| --------------------------- | ------------------------------------------- |
| **当前 JD** 下拉                | 选这一轮用哪个岗位模板去评估候选人                           |
| **候选人来源** 下拉                | 选「推荐」或「最新」                                  |
| **招呼数** 输入框                 | 评估「符合」累计达到这个数就停（自动招呼上限）                     |
| **浏览人数** 下拉                 | 候选人总评估数上限（15 的倍数，从 15 到 600）；两个数任一先达到就停    |
| **自动招呼** toggle             | 关掉则只评估不发招呼（适合试运行）                           |
| **开始本轮** / **停止本轮**         | 启动 / 中止流程                                   |
| 候选人卡片 — 点**姓名**             | BOSS 页面平滑滚动到这个人并高亮 1.5 秒                    |
| 候选人卡片 — 点 **▶ 箭头**（pass / 符合 都有）| 展开看「为什么 pass / 符合」+ 每条 M/O 条件的 LLM 详细判断（v0.19.0 后符合也支持展开）|
| **📊 看板** 按钮                | 打开数据看板（新标签页）                                |
| **⚙️ 设置**                   | 打开 admin 配置页                                |

> v0.20.6 起：原「清空评估」/「清空全部」按钮已迁到 admin 底部「危险操作」区（带双重 confirm），平时不需要主动清；「开始本轮」会自动清评估列表。

### admin 配置页（v0.17.1.x 新增 section）

**话术模板管理** + **评估后自动求简历** 两个 section 是 v0.17.1.0 新增：

| 区域 | 按钮 / 控件 | 作用 |
|---|---|---|
| **话术模板管理** | + 新建话术 / 编辑 / 删除 | CRUD 话术（要 ≥5 字符），sidepanel 选当前用哪条 |
| **评估后自动求简历** | 启用批量评估自动求简历 | 开了 batch 评估自动跑话术+求简历（单评始终手动） |
| | 试跑模式 (dry-run) | 跑通所有定位但不点最后的发送 / 确定，调试 BOSS UI 改名用 |
| | 候选人间冷却下限 / 上限（毫秒） | 自动模式专属冷却（默认 2-4s） |

### admin 配置页 — 原有 section

| 区域                | 按钮 / 控件              | 作用                                  |
| ----------------- | -------------------- | ----------------------------------- |
| 顶部                | **打开看板**             | 跳转到数据看板（漏斗 + Pass 主因）               |
| **大模型配置**         | **+ 新增大模型**          | 加新的 LLM 配置（Wiz / DeepSeek / 自定义）    |
|                   | 列表条目「**设为当前**」       | 切换使用哪个 LLM 评估（即时生效）                 |
|                   | 列表条目「**测试**」         | 测试这条配置能否调通 LLM                      |
|                   | 列表条目「**编辑**」         | 改这条配置                               |
|                   | 列表条目「**删除**」         | 删这条配置                               |
|                   | drawer「**测试连接**」     | 当前 form 字段临时测试一次（不写入列表）             |
|                   | drawer「**保存**」       | 保存当前编辑的 LLM 配置（实时写入）                |
|                   | drawer「**取消**」       | 关闭抽屉不保存                             |
| **JD 模板管理**       | **新建 JD**            | 新建岗位模板（必要条件 M + 可选条件 O + 阈值 K）      |
|                   | 列表行「**编辑** / **删除**」 | 改 / 删某条 JD                          |
|                   | 列表行「●**当前**」标记       | 标记当前 JD（侧边栏切换的同步显示）                 |
|                   | 「**预览 prompt**」      | 看选中 JD 实际给 LLM 的 system prompt 长什么样 |
| **自动打招呼（保持默认即可）** | **单条间隔（最小 / 最大）**    | 招呼之间随机间隔（默认 1.5-5 秒）                |
|                   | **每 N 个休息**          | 每发 N 个招呼后停一会儿（默认 30 个）              |
|                   | **休息（分钟）**           | 休息时长（默认 5-10 分钟随机）                  |
| 顶部 / 底部           | **保存所有更改**           | 保存 sayHi 参数（LLM 配置已实时保存，不需要这个按钮）    |
|                   | **导出 JD 模板 JSON**    | 备份所有 JD 模板到文件                       |
|                   | **导入 JD 模板 JSON**    | 恢复 JD 模板                            |

### 看板页面（v0.20.x 看板 v2 决策仪表盘）

5 视图 ABCDE：

| 视图 | 作用 |
|---|---|
| **4.B 双场景漏斗** | 顶部最大块，推荐页 / 沟通页 tab 切换。3 级：已判断 → 符合 → 自动招呼发出。点漏斗格跳 4.E 候选人记录 |
| **4.C JD 分析** | 上「必要条件 M_i」每条 pass 主因 + 信息缺占比；下「可选条件 O_i」命中率。每条带「去 admin 改」按钮一键跳转 |
| **4.D 7/8/6 天趋势** | 跟漏斗对齐三指标：判断候选人数 / 符合数 / 自动招呼发出数 |
| **4.E 候选人记录** | 默认展示，超出 28 条加「⋯ 导出 CSV」提示。顶部 [显示全部 / 仅符合] tab |

**顶部控件**：
| 按钮 | 作用 |
|---|---|
| **日 / 周 / 月** | 时间窗，切片所有视图 |
| **JD 下拉** | 看哪个岗位的数据（含全部 + 3 SEED） |
| **场景 tab**（漏斗内嵌） | 推荐页 / 沟通页（沟通页主链路完工但漏斗 L3/L4 埋点未补，目前占位） |

---

**完。**

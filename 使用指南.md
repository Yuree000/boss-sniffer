# BOSS Sniffer · HR 使用指南

> 版本 **v1.1.25**（2026-05-25）｜ 适用：Chrome 浏览器 + BOSS 直聘 HR 端

---

## 这是什么

一个 Chrome 扩展，帮 HR 在 BOSS 直聘上**用 LLM 自动筛选候选人**：

- **推荐页**：扩展自动翻页 → 调 LLM 评估每个候选人 → 符合的自动打招呼
- **沟通页**：扫描「新招呼」里的候选人 → LLM 评估 → 「符合」自动求简历、「不符合」一键标不合适

一个 BOSS 岗位下可以挂**多份筛选模板**（比如「AI Builder · 偏前端 / 偏后端」），每个候选人对所有模板**并行评估**，不漏人。

---

## 1. 安装（首次 4 步）

1. 下载本仓库压缩包 → 解压到本地任意目录（建议 `D:\boss-sniffer\`）
2. Chrome 地址栏访问 `chrome://extensions/` → 右上角打开**「开发者模式」**
3. 左上角点**「加载已解压的扩展程序」** → 选择刚解压的 `boss-sniffer` 文件夹
4. Chrome 工具栏点扩展图标（蓝色 BS 图标）→ **「固定」** 让它常驻

完成后浏览器右侧会出现「BOSS Sniffer」侧边栏入口。

---

## 2. 首次配置（3 步）

### 2.1 配 LLM（大模型 API）

侧边栏右上角点 **⚙ 设置** 进入配置页 → 左侧「LLM 配置」：

| 字段 | 填什么 |
|---|---|
| Provider | `Wiz`（内部代理，推荐）/ `Anthropic` / `DeepSeek` 任选 |
| API Key | 找开发同事拿（不同 provider 不一样的 key） |
| Model | `claude-haiku-4-5-20251001`（推荐，速度快质量好）/ `claude-sonnet-4-6`（贵但更准） |
| Concurrency | `5`（同时跑几个 LLM 请求，太高会限流） |

保存后点「测试连接」，看到 ✅ 代表通了。

### 2.2 创建 BOSS 岗位

「BOSS 岗位」是顶层，对应 BOSS 直聘上你实际发布的招聘岗位。

设置页 → **「BOSS 岗位」** 标签 → 点「+ 新增岗位」：

- 岗位名（必填）：**必须跟 BOSS 上候选人「沟通职位」字段一字不差**（比如「印尼语实习生」、「AI Builder」）
- 否则路由匹配不上，候选人会被标记「未识别岗位」跳过 LLM 评估

### 2.3 给岗位加筛选模板

每个 BOSS 岗位下至少加 1 份筛选模板：

设置页 → **「JD 模板」** 标签 → 选某个岗位 → 点「+ 新增模板」：

| 字段 | 说明 |
|---|---|
| **必要条件**（M1, M2, ...） | 全部都要满足才能算「符合」（比如：本科及以上、3 年经验） |
| **可选条件**（O1, O2, ...） | 满足达标数即可（比如 5 条中过 3 条） |
| **达标数** | 可选条件至少要满足几条 |
| **默认打招呼话术** | 「符合」时自动发的开场白 |
| **自定义 prompt** | 给 LLM 的额外指令（可选，比如「只看简历，忽略招呼话术」） |

⚠️ 改完条件后**保存即生效**。已经评估过的候选人**下次再扫到时**会被自动重新评估（因为筛选条件指纹变了）。

---

## 3. 推荐页用法（持续招呼 N 个候选人）

适用：你只想招呼 N 个候选人然后停，不想 24h 自动跑。

### 3.1 配置

1. 切到 BOSS 直聘**「推荐」**页（看到一个候选人卡片为主屏的那个）
2. 打开扩展侧边栏 → **「推荐页」** tab
3. 顶部「BOSS 岗位」下拉选**当前要招呼的岗位**
4. 「使用模板」勾选要参与评估的模板（默认全选，可以「全选」一键勾上）
5. **N**（目标招呼人数）：比如填 `20` = 招呼到 20 个符合的就停
6. **K**（最大浏览轮数）：比如填 `30` = 最多翻 30 轮（450 人）还没凑齐 N 就停
7. **目标 tab**：通常选「推荐」

### 3.2 启动

点 **▶ 开始**。扩展开始自动翻页 + 评估：

- 状态徽章变绿「本轮运行中」
- 进度行：「进度：已招呼 5/20 · 已浏览 90/450」
- 中间穿插「休息中 02:35」防风控

### 3.3 候选人卡片

每翻一页，扫到的候选人会出现在侧边栏列表：

- **「符合 3 / 4 模板」**：4 个模板中 3 个判定符合（绿底）
- **「符合 0 / 4 模板」**：4 个全 pass（红底）
- 点候选人卡片 → 展开多模板汇总
- 再点某个模板子行 → 展开看 **LLM 给的具体理由**（v1.1.24 新加） + 每条 M/O 条件 ✓/✗ + LLM 解释
- 失败评估也能展开看错误信息 + 重试轨迹

### 3.4 停止 / 暂停 / 续跑

- **⏸ 暂停**：保留进度，可点「▶ 续跑」继续
- **⏹ 停止**：本轮结束，要重新跑就点 ▶ 开始

---

## 4. 沟通页用法（处理「新招呼」批次）

适用：你想批处理 BOSS 里已有的「新招呼」消息，一次性筛掉不合适的、给合适的求简历。

### 4.1 ⚠️ 前提：必须在「新招呼」tab 下（v1.1.25 起强制）

1. 打开 BOSS 直聘 → 顶部点**「沟通」**
2. **顶部一级 tab 切到「新招呼」**（蓝色高亮 + 角标数字）
3. **不能在「全部」/「沟通中」/「已回应」等其他 tab 下用**

  > **v1.1.25 起**：在其他 tab 下点「开始处理本批」会被拦下，弹 toast 提示「当前在『X』tab，筛选只能在『新招呼』tab 下进行」。
  >
  > 旧版本（≤v1.1.24）没有拦截，会把已经聊过的候选人当新招呼跑评估，导致结果错乱。建议确认扩展版本 ≥ v1.1.25。

### 4.2 操作

打开扩展侧边栏 → **「沟通页」** tab：

1. 配置浏览数 K（本批最多评估几人，留空 = 全跑）
2. 勾选自动操作：
   - ☑ **自动发送话术 + 求简历**（评估「符合」时）
   - ☑ **自动标不合适**（评估「不符合」时）
3. 点 **▶ 开始处理本批**

### 4.3 进度面板

启动后进度行实时更新：

```
评估中 12 / 39（30%）· 平均 2.8 秒/人 · 还需 1 分 16 秒
```

候选人列表实时刷新：

- ✓ 已招呼（绿色 badge）
- ⊘ 已超 N（本轮配额满了，不再招呼）
- ⚠ 招呼失败（点开看错误）

### 4.4 「标错」反馈通道

如果 LLM 判定「符合」但你看了简历觉得不对：

- 候选人卡片右上角的「**标错**」按钮 → 点一下，变红 ⚠「已标错」
- 数据流向看板，后续用于训练改进 prompt

---

## 5. 看板（评估历史 / 漏斗 / 反馈）

侧边栏右上角点 **📊 看板** → 在新 tab 打开 dashboard：

| Tab | 看什么 |
|---|---|
| **决策仪表盘** | KPI 卡（处理速率 / 节省人力 / 符合率 / 通过率） |
| **明细抽屉** | 全部候选人评估记录，可按 JD / 决策 / 时间过滤 |
| **LLM 错判** | 你标过「标错」的候选人专列 |
| **事件流** | 每个动作（招呼 / 标不合适 / 求简历）的精确时间戳 |
| **漏斗** | 候选人 → 评估 → 招呼 → 已回应 → 已交换电话 / 微信 的转化率 |

明细抽屉可以**导出 CSV** 给老板看。

---

## 6. 常见问题 / 错误码

| 现象 | 原因 / 处理 |
|---|---|
| 「当前在『全部』tab，筛选只能在『新招呼』tab 下进行」| 切到 BOSS 沟通页顶部「新招呼」一级 tab 再点开始 |
| 「未扫到候选人」 | 候选人列表为空 / DOM 没加载完，刷新 BOSS 页等 2 秒再试 |
| 候选人决策标「未识别岗位」 | 你 BOSS 岗位名跟候选人的「沟通职位」不一字不差 → 设置页改岗位名对齐 |
| 「评估失败」+ 重试 3 次 | LLM 网络问题 / API key 错 / 配额超 → 点候选人卡展开看具体错误，到设置页测试连接 |
| 「⏱ 已 60s」红字 | LLM 跑慢了，可能在重试。耐心等或停掉重启 |
| 「⊘ 已超 N」 | 本轮目标招呼数已满，符合的候选人不再入招呼队列 |
| 看板打不开 / 数据是空 | service worker 重启时数据库需 5 秒加载，等等再点；持续不行联系开发 |
| LLM 标错了（明显不符合却给了「符合」）| 候选人卡右上点「标错」反馈 → 开发会调 prompt |

### 紧急停手

随时点 **⏹ 停止本轮** 或 **■ 停止**。如果扩展不响应：

1. 刷新 BOSS 页面（F5）
2. `chrome://extensions/` 找 BOSS Sniffer → **🔄 刷新**
3. 还不行 → 拷整段错误（看板「诊断日志」可导出 JSON）发开发

---

## 7. 版本号 + 反馈

**当前版本**：v1.1.25（2026-05-25）

**查版本**：`chrome://extensions/` → 找 BOSS Sniffer → 看「版本」字段

**反馈渠道**：

- 功能建议 / bug 报告 → 找开发同事（描述时附「在哪个页面 / 点了什么 / 看到什么 / 期望看到什么」四点）
- 诊断包导出：看板 → 「导出诊断」 → 生成 `boss-sniffer-diag-YYYYMMDD-HHMM.json` → 直接发给开发

---

## 附录：本版本（v1.1.25）相比 v1.1.20 的主要变化

- **v1.1.23**：引入 BOSS 岗位两层数据模型 — 一个岗位下可挂多份筛选模板，对每个候选人并行评估所有模板（不漏「偏前端 / 偏后端」的判断）
- **v1.1.24**：候选人卡片每个模板子行升级为可点击展开 — 看 **LLM 给的具体理由** + 逐条 M/O 条件结果（v1.1.23 改造时漏渲，已找回）；删除「全不选」按钮（无意义）
- **v1.1.25**：沟通页强制只能在「新招呼」一级 tab 下启动（修历史 bug：在其他 tab 下也能扫候选人导致筛选错乱）

# BOSS Sniffer · HR 使用指南

> 版本 **v1.1.25**（2026-05-25）｜ 适用：Chrome 浏览器 + BOSS 直聘 HR 端

---

## 这是什么

一个 Chrome 扩展，帮 HR 在 BOSS 直聘上**用 LLM 自动筛选候选人**：

- **推荐页**：扩展自动翻页 → 调 LLM 评估每个候选人 → 符合的自动打招呼
- **沟通页**：扫描「新招呼」里的候选人 → LLM 评估 → 「符合」自动求简历、「不符合」一键标不合适

**一个 BOSS 岗位下可以挂多份筛选模板**（比如同一个「AI Builder」岗位下挂「偏前端 / 偏后端 / 偏算法」3 份模板），每个候选人对**所有模板逐一评估**，任何一个模板判定「符合」就触发自动招呼，不漏人。

---

## 1. 安装（首次 4 步）

1. 下载本仓库压缩包 → 解压到本地任意目录（建议 `D:\boss-sniffer\`）
2. Chrome 地址栏访问 `chrome://extensions/` → 右上角打开**「开发者模式」**
3. 左上角点**「加载已解压的扩展程序」** → 选择刚解压的 `boss-sniffer` 文件夹
4. Chrome 工具栏点拼图图标 → 在列表里找到 **BOSS Sniffer** → 点📌让它常驻

完成后浏览器右侧栏入口出现「BOSS Sniffer」，点扩展图标可打开 / 关闭侧边栏。

---

## 2. 首次配置

设置页打开路径：扩展工具栏图标 → 右键 → **「选项」**（或：`chrome://extensions/` → 找到扩展 → 「详情」 → 「扩展程序选项」）

### 2.1 大模型配置

设置页 → **「大模型配置」** section：

| 字段 | 填什么 |
|---|---|
| Provider | `Wiz`（内部代理，推荐）/ `Anthropic` / `DeepSeek` 任选 |
| API Key | 找开发同事拿（不同 provider 不一样的 key） |
| Model | `claude-haiku-4-5-20251001`（推荐，速度快质量好）/ `claude-sonnet-4-6`（更准但贵） |
| Concurrency | `5`（同时跑几个 LLM 请求，太高会限流） |

保存后点「测试连接」，看到 ✅ 代表通了。

### 2.2 新建 BOSS 岗位

设置页 → **「JD 模板管理」** section（注意：BOSS 岗位**不是独立标签**，是嵌在 JD 管理里的树形结构）→ 点 **「+ 新建岗位」**：

- 岗位名（必填）：**必须跟 BOSS 上候选人「沟通职位」字段一字不差**（比如「印尼语实习生」、「AI Builder」）
- 否则路由匹配不上，候选人会被标记「未识别岗位」跳过 LLM 评估

新建后岗位以**卡片形式**展示，可展开 / 折叠 / 改名 / 删除。

### 2.3 给岗位加筛选模板（重点：一个岗位可加多个模板）

**关键能力**：一个 BOSS 岗位下可以**挂任意多份筛选模板**。比如「AI Builder」岗位下可同时挂「偏前端」「偏后端」「偏算法」3 份模板，每个候选人会**对 3 份模板都跑一遍 LLM**，任何一个判「符合」就触发自动招呼。

操作：在岗位卡片内点 **「+ 添加模板」** → 弹出模板编辑器：

| 字段 | 说明 |
|---|---|
| 模板名 | 用来区分同岗位下的不同模板（比如「偏前端」、「偏后端」） |
| **必要条件**（M1, M2, ...） | 全部都要满足才能算「符合」（比如：本科及以上、3 年经验） |
| **可选条件**（O1, O2, ...） | 满足达标数即可（比如 5 条中过 3 条） |
| **达标数** | 可选条件至少要满足几条 |
| **话术模板** | 「符合」时自动发的开场白。**支持挂多条话术**，选一条设为「默认」 |
| **自定义 prompt** | 给 LLM 的额外指令（可选） |

加完 1 个，**再点「+ 添加模板」继续加第 2/3/4 个**。每个模板独立编辑、独立保存。

模板列表行右侧操作按钮：
- **📋 复制** — 一键克隆当前模板（条件 / 话术全复制），改名后保存就是同岗位下的新模板
- 预览 / 编辑 / 删除 / 拖拽排序

⚠️ 改完条件保存即生效。已经评估过的候选人**下次再扫到时**会被自动重新评估（系统对筛选条件算了内容指纹，指纹变了就强制重评）。

---

## 3. 推荐页用法（持续招呼 N 个候选人）

适用：你只想招呼 N 个候选人然后停，不想 24h 自动跑。

### 3.1 配置

1. 切到 BOSS 直聘**「推荐」**页（候选人卡片为主屏）
2. 打开扩展侧边栏 → **「推荐页」** tab
3. 顶部「BOSS 岗位」下拉选**当前要招呼的岗位**（必选，否则不知道路由到哪个 JD 树）
4. 「使用模板（多选，N 个模板 = N 张评估卡）」chip — 默认全勾，可点 **「全选」** 一键勾上
5. **N（目标招呼人数）**：比如填 `20` = 招呼到 20 个符合的就停
6. **K（最大浏览轮数）**：下拉选 `15 / 30 / ... / 450`（一轮约 15 人）
7. **目标 tab**：`推荐（默认）` 或 `最新` — 决定扩展自动翻哪个 tab

### 3.2 启动

点 **▶ 开始本轮**。扩展开始自动翻页 + 评估：

- 顶部状态徽章变绿「本轮运行中」
- 进度行：「进度：已招呼 5/20 · 已浏览 90/450」
- 中间穿插「休息中 02:35」防风控

### 3.3 候选人卡片（评估结果）

每翻一页扫到的候选人出现在列表，每张卡片左侧颜色 + 文案表示状态：

- 🟢「符合 3 / 总 4 模板」：4 个模板里 3 个判符合
- 🔴「符合 0 / 总 4 模板」：4 个全 pass
- ⏳ pending / 待评估
- 🟡「未识别岗位」：沟通职位跟你 BOSS 岗位名对不上

**展开看 LLM 理由**（v1.1.24 找回的功能）：

1. 点候选人卡片整体 → 展开多模板汇总
2. 点某个模板**子行**（行右侧 ▶ 箭头）→ 展开看：
   - 📋 LLM 一句话总结理由
   - 每条 M1/M2... O1/O2... 条件 ✓/✗ + LLM 给的具体解释
3. 失败评估展开看错误信息 + 重试轨迹

候选人卡片右上角按钮：
- **标错** — 如果 LLM 判「符合」但你觉得不对，点一下变红 ⚠「已标错」，进看板用于改 prompt

### 3.4 停止 / 续跑

- **■ 停止本轮**：本轮结束（保留已收集数据），要重新跑就再点 ▶ 开始本轮
- **▶ 继续**：扩展在被风控暂停时显示（自动进入 PAUSED 状态），点击恢复

---

## 4. 沟通页用法（处理「新招呼」批次）

适用：批处理 BOSS「新招呼」里已经到来的候选人，一次性筛掉不合适的、给合适的求简历。

### 4.1 ⚠️ 前提：必须在「新招呼」一级 tab 下（v1.1.25 起强制）

1. 打开 BOSS 直聘 → 顶部点**「沟通」**
2. **顶部一级 tab 切到「新招呼」**（蓝色高亮 + 角标数字，比如「新招呼 (12)」）
3. **不能在「全部」/「沟通中」/「已回应」/「已获取简历」等其他一级 tab 下用**

> **v1.1.25 起**：在其他 tab 下点「开始处理本批」会被拦下，弹 toast：「当前在『X』tab，筛选只能在『新招呼』tab 下进行」。
>
> 旧版本（≤v1.1.24）没有这道闸门，会把已经聊过的候选人当新招呼跑评估，结果错乱。建议确认扩展版本 ≥ v1.1.25。

### 4.2 操作

打开扩展侧边栏 → **「沟通页」** tab：

1. 「浏览数」输入框（placeholder「全部」）—— 本批最多评估几人，留空 = 处理本批所有未读
2. 勾选自动操作：
   - ☑ **自动发送话术 + 求简历**（评估「符合」时）
   - ☑ **自动标不合适**（评估「pass」时）
3. 点 **▶ 开始处理本批**

### 4.3 进度面板

启动后实时更新：

```
评估中 12 / 39（30%）· 平均 2.8 秒/人 · 还需 1 分 16 秒
```

候选人卡片实时刷新，右侧状态 badge：

- ✓ **已招呼**（绿色，已发话术 + 求简历）
- ⊘ **已超 N**（本轮配额满了，不再招呼。配额从推荐页那边的 N 复用，沟通页也会受限制）
- ⚠ **招呼失败**（点开看错误）

### 4.4 「标错」反馈通道

跟推荐页一样：

- 候选人卡片右上角的 **标错** 按钮 → 点一下变红 ⚠「已标错」
- 数据汇总到看板的「🚩 仅 LLM 错判」过滤项，方便后续找出来批量改 prompt

---

## 5. 看板（评估历史 / 漏斗 / 反馈）

侧边栏顶部「📊 看板」按钮 → 在新 tab 打开 dashboard：

### 5.1 顶部：场景 tab 切换

| Tab | 数据范围 |
|---|---|
| **推荐页** | 推荐页自动招呼的候选人评估记录 |
| **沟通页** | 沟通页「新招呼」批次评估的候选人记录 |

### 5.2 每个场景下的内容（自上而下）

- **🩺 JD 分析**：当前 JD 的累计评估、符合率、招呼率统计
- **📈 天趋势**：按天的评估量 / 符合数趋势图
- **⏱️ 批次效率**：每批次的吞吐量（平均耗时 / 总耗时 / 速率）
- **候选人抽屉**：所有评估记录列表，顶部过滤 tab：
  - 「显示全部」/「仅符合」/「🚩 仅 LLM 错判」
- **导出 CSV**：抽屉顶部 **📥 导出 CSV (N)** 按钮，把抽屉里全部候选人导出来给老板看
- **⚙ 诊断**（点击展开 — 开发用）：诊断日志预览，HR 不用看

### 5.3 候选人详情 modal

抽屉里点某个候选人 → 弹出详情 modal，三个 tab：

- **👤 基础资料**：BOSS 抓到的候选人原始字段
- **💼 履历**：教育 / 工作经历 / 聊天历史
- **🤖 LLM 评估**：每个模板的判定 + 逐条条件 ✓/✗ + 理由

---

## 6. 常见问题 / 错误码

| 现象 | 原因 / 处理 |
|---|---|
| 「当前在『全部』tab，筛选只能在『新招呼』tab 下进行」| 切到 BOSS 沟通页顶部「新招呼」一级 tab 再点开始 |
| 「未扫到候选人」 | 候选人列表为空 / DOM 没加载完，刷新 BOSS 页等 2 秒再试 |
| 候选人决策标「未识别岗位」 | BOSS 岗位名跟候选人「沟通职位」不一字不差 → 设置页改岗位名对齐 |
| 「评估失败」+ 重试 3 次 | LLM 网络问题 / API key 错 / 配额超 → 点候选人卡展开看具体错误，到设置页测试连接 |
| 「⏱ 已 60s」红字 | LLM 跑慢了，可能在重试。耐心等或停掉重启 |
| 「⊘ 已超 N」 | 本轮目标招呼数已满，符合的候选人不再入招呼队列 |
| 看板打不开 / 数据是空 | service worker 重启时数据库需 5 秒加载，等等再点；持续不行联系开发 |
| LLM 标错了（明显不符合却给了「符合」）| 候选人卡右上点「标错」反馈 → 开发会调 prompt |
| 同一候选人 JD-A 已评估，换 JD-B 后还不重评 | v1.1.22 之前的 bug，**v1.1.22 起已修**：JD 切换强制重评 |
| HR 改了筛选条件，旧评估还按老条件显示 | v1.1.22 起：保存模板时自动算条件指纹，下次扫到该候选人会强制重评 |

### 紧急停手

随时点 **■ 停止本轮**（推荐页）或 **■ 停止**（沟通页）。如果扩展不响应：

1. 刷新 BOSS 页面（F5）
2. `chrome://extensions/` 找 BOSS Sniffer → **🔄 刷新**
3. 还不行 → 导出诊断包发开发（导出方法见 §7）

---

## 7. 版本号 + 反馈 + 诊断包导出

**当前版本**：v1.1.25（2026-05-25）

**查版本**：侧边栏右上角的版本 tag（比如 `v1.1.25`）；或 `chrome://extensions/` → BOSS Sniffer 卡片「版本」字段

### 7.1 导出诊断包（开发排查时用）

**位置**：侧边栏右上角的**版本号 tag**（`v1.1.25` 那个标签）→ **快速三连点**（1 秒内点击 3 次）→ 自动下载 `boss-sniffer-diag-YYYYMMDD-HHMM.json`

诊断包含最近的事件日志、评估记录摘要、当前配置（不含 API key），把它直接发给开发就能复现问题。

### 7.2 反馈渠道

- 功能建议 / bug 报告 → 找开发同事，描述时附四点：「在哪个页面 / 点了什么按钮 / 看到了什么 / 期望看到什么」
- 加上诊断包，开发能精准定位

---

## 附录：本版本（v1.1.25）相比 v1.1.20 的主要变化

- **v1.1.22**：候选人 dedup 跟 JD 解耦 — JD 切换 / 筛选条件变更强制重评，不再因 candidateId 重复就漏判；JD 模板支持「复制」一键克隆
- **v1.1.23**：引入 BOSS 岗位两层数据模型 — **一个岗位下可挂多份筛选模板**，对每个候选人**逐一评估所有模板**（任何一个符合就招呼），不再漏「偏前端 / 偏后端」之类的差异
- **v1.1.24**：候选人卡片每个模板子行升级为可点击展开 — **找回 LLM 给的具体理由 + 逐条 M/O 条件结果**（v1.1.23 改造时漏渲）；删除「全不选」按钮（无意义）
- **v1.1.25**：沟通页**强制只能在「新招呼」一级 tab 下启动**（修历史 bug：在其他 tab 下也能扫候选人导致筛选错乱）
